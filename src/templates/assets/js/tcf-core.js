var tcfLib;
readyDOM(() => {
  jQuery(document).ready($ => {

    tcfFunction = {
      initEvent: () => {
        // Handle event        
      }
    }

    // Init Event Element
    setTimeout(function () {
      tcfFunction.initEvent();
    }, 500);

  });
}, false);